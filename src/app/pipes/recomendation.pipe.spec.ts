import { RecomendationPipe } from './recomendation.pipe';

describe('RecomendationPipe', () => {
  it('create an instance', () => {
    const pipe = new RecomendationPipe();
    expect(pipe).toBeTruthy();
  });
});
