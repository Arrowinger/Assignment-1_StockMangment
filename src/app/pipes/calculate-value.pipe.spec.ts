import { CalculateValuePipe } from './calculate-value.pipe';

describe('CalculateValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateValuePipe();
    expect(pipe).toBeTruthy();
  });
});
